<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>
<% if user_signed_in? %>
  <%= render "shared/manage_newsletter", name: "email_blast", user: current_user %>
  <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete} %>
<% else %>
  <%= link_to "Sign In", new_user_session_path %>
<% end %>
<%= button_to "Email Users", email_blast_path(name: "email_blast"), method: :post, data: {turbo_method: :post} %>
